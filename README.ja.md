# Taiwan Legal RAG：台湾法律 MCP サーバー & CLI（`twlegalrag`）

<div align="center">

### 🌐 Language / 語言 / 言語

[**English**](README.en.md) ・ [**繁體中文**](README.md) ・ **日本語**

</div>

---

> **無料・登録不要・API キー不要**の台湾法律 **MCP サーバー**：Claude、ChatGPT、Gemini CLI、Microsoft 365 Copilot、Codex、Cursor に
> `https://tlr.dr-legal.com.tw/mcp` を追加するだけで、AI アシスタントが 2,250 万件の台湾裁判書・
> 行政解釈・憲法法廷判決をセマンティック検索し、引用チェック付きで返します。
> 同じバックエンドに接続するソースコード公開 CLI（`twlegalrag`）も提供。

Taiwan Legal RAG CLI は、法律偵探の公開 TLR エンドポイントから台湾の裁判所判決を
検索し、**あなた自身の AI ツール**で使える形にパッケージするツールです。本ツールは
法的助言を**生成せず**、いかなる LLM も**呼び出さず**、第三者モデルの出力の意味的
忠実性を**保証しません**。内蔵の引用チェックは、引用された判決が取得済みバンドルに
含まれるかどうかのみを検証します。

## データカバレッジ（2026-09-01 時点、production データベースから直接集計）

| コーパス | 件数 | アクセス方法 |
|---|---:|---|
| **裁判書**(台湾の全審級) | **22,558,169 件** | セマンティック＋語彙検索＋事件番号完全一致照会；司法院オープンデータを毎日増分同期（公開後の訂正・取下げも同期） |
| 審級関係(上訴チェーン) | 4,537,220 件超 | 判決ごとに `case_history` として付帯(主文「廢棄/駁回」フラグ付き) |
| 行政規則(行政函釋・令函) | 84,737 件 | 発文番号の完全一致照会＋セマンティック検索(hosted MCP)、省庁別内訳は下表 |
| 大法官解釈 | 813 件 | 同上 |
| 憲法法廷裁判 | 57 件 | 同上 |
| 税務解釈(財政部) | 9,093 件 | 同上 |
| 解釈の効力台帳 | 50,853 件超 | 廃止/適用停止/差替え状態を引用前に確認 |
| 不当労働行為裁決(労働部裁決委員会) | 400 件 | 労働関連クエリに併記(裁判所判決ではない旨を明示) |
| 憲法 | 1 件 | 中華民国憲法 197 条(増修条文 12 条を含む)。[dr-legal.com.tw](https://dr-legal.com.tw) で検索可能 |
| 法律 | 1,083 件/45,620 条 | 中央法規標準法 §2 に定める「法・律・条例・通則」。同上 |
| 命令 | 7,474 件/132,760 条 | 同法 §3 に定める「規程・規則・細則・辦法・綱要・標準・準則」。同上 |
| 廃止法令 | 3,230 件 | 法律 254 件、命令 2,974 件、及び動員戡亂時期臨時条款など憲法位階の規範。沿革調査用に廃止状態を明示 |

裁判書は毎日増分同期(司法院オープンデータの公開には数日の遅延があります)。
上記の数値は記載日に production データベースから直接取得したものです。

### 裁判書の内訳(審級/事件類型別)

| 法院層級 | 筆數 |
|---|---:|
| 地方法院 | 16,686,132 |
| 地方法院簡易庭 | 3,268,493 |
| 高等法院及分院 | 1,328,658 |
| 最高法院 | 399,288 |
| 高等行政法院 | 200,600 |
| 最高行政法院 | 122,964 |
| 地方行政訴訟庭 | 78,891 |
| 智慧財產及商業法院 | 23,678 |
| 高雄少年及家事法院 | 22,113 |
| 其他專業法庭・委員會 | 32,250 |
| 未帶法院代碼欄位（計入總數，不列層級） | 395,102 |
| **合計** | **22,558,169** |

| 案件類別 | 筆數 |
|---|---:|
| 民事 | 14,232,700 |
| 刑事 | 7,332,300 |
| 行政 | 573,417 |
| 其他 | 24,650 |

### 行政規則の発出機関別内訳(発文番号順 75,644 件)

機関名は各解釈に記録された正式な中国語名のまま掲載しています(改組前の
歴史的名称を含む)。

| 機関 | 件数 |
|---|---:|
| 財政部 | 10,623 |
| 內政部國土管理署 | 8,769 |
| 經濟部智慧財產局 | 7,161 |
| 勞動部 | 7,147 |
| 法務部 | 7,068 |
| 行政院環境保護署 | 4,463 |
| 行政院公共工程委員會 | 4,104 |
| 銓敘部 | 3,990 |
| 經濟部 | 3,131 |
| 農業部 | 3,078 |
| 金管會 | 2,824 |
| 內政部 | 2,654 |
| 前司法行政部 | 1,432 |
| 法務部行政執行署 | 1,410 |
| 內政部戶政司 | 1,391 |
| 公務人員保障暨培訓委員會 | 687 |
| 主計總處 | 669 |
| 國科會 | 568 |
| 文化部文化資產局 | 561 |
| 農業部水保署 | 543 |
| 司法行政部 | 433 |
| 考選部 | 429 |
| 人事行政總處 | 323 |
| 核能安全委員會 | 228 |
| 原住民族委員會 | 225 |
| 海洋委員會 | 223 |
| 公平交易委員會 | 204 |
| 文化部 | 203 |
| 法務部矯正署 | 168 |
| 中央選舉委員會 | 112 |
| 農業部林業及自然保育署 | 103 |
| 客家委員會 | 97 |
| 國家發展委員會 | 86 |
| 考試院 | 86 |
| 個人資料保護委員會籌備處 | 73 |
| 故宮博物院 | 58 |
| 環境部 | 46 |
| 臺灣高等法院檢察署 | 41 |
| 法務部政風司 | 37 |
| 法務部廉政署 | 32 |
| 司法院 | 28 |
| 經濟部能源署 | 28 |
| 法務部調查局 | 20 |
| 其他 35 個機關（各未滿 20 筆） | 88 |
| **合計** | **75,644** |


## 何が違うのか

一般的なキーワード判決検索ツールではありません。背後にあるのは Legal Detective が
長期にわたり構築してきた TLR 検索サービスです：

- **22,558,169 件**の台湾裁判データ（2026-09-01 時点）を構造化処理・ベクトル化。
- **セマンティック検索**：自然言語で「概念は近いが言い回しが違う」判決を発見。
  専門用語向けの語彙完全一致モードも利用可能。
- **事件番号の完全一致照会**：完全な事件番号は自動で完全一致照会に切替。
  該当なしの場合は「存在しないことを意味しない」と明示し、類似結果で埋めません。
- **審級関係 `case_history`**：主文「廢棄/駁回」フラグ付きで上下審を付帯。
  **引用前に**上級審で破棄済みかどうかを確認できます。
- **行政解釈ツール**：発文番号の完全一致照会（効力状態付き）とセマンティック
  検索。解釈と判決は厳格に分離。詳細は [`docs/mcp-anchor.ja.md`](docs/mcp-anchor.ja.md)。
- **現行法条の完全一致照会**：法令名＋条番号で現行条文の原文を取得。法令の
  最終改正日と廃止注記付き。条番号の実在と条文内容を引用前に検証でき、モデルの
  記憶に頼る必要がありません。現行整備版のみ（改正前の旧条文は公式の沿革を参照）。
- **引用ガードは第一級市民**：`allowed_citations` ホワイトリスト、
  `unread_candidates` マーカー、bundle ごとの検証指示、CLI 側の引用チェック。
  「番号は本物、見解は捏造」という法務 AI 最悪の幻覚類型に対する設計です。
- 本 CLI は判決データベースを**同梱せず**、モデル重み・ベクトル索引・検索
  パイプラインの内部も公開しません。

### 「公式サイトラッパー」型ツールとの違い

もう一つの一般的な手法は、司法院・法令の公式サイト内検索へリアルタイムに
転送するものです。両者は役割が異なり、補完関係にあります：

| | 公式サイトラッパー | Taiwan Legal RAG |
|---|---|---|
| 検索方式 | 公式サイト内キーワード検索 | 自前構築の 2,200 万件コーパスに対するセマンティック検索。概念が近ければ用語が違っても見つかる |
| 引用保護 | 通常なし | read-whitelist + 検証指示 + 引用チェック |
| 事件番号取寄せ | サイトの機能に依存 | 正確検索。見つからない場合は「推測禁止」を明示 |
| 審級関係 | 事件ごとに自分で追跡 | `case_history` が自動付属、破棄フラグ付き |
| 可用性 | サイトの WAF・改版の影響を受け、ローカルブラウザで認証回避が必要なことも | ホスト型エンドポイント、ローカル環境不要 |
| 即時性 | 公式サイトはリアルタイム | ごく最近公告された裁判は公式サイトを優先 |

ラッパー型の強みは即時性と公式ソース直結、本ツールの強みはセマンティック検索の
品質と引用規律です。

（補足：ソースコードを公開しているのは **CLI** であり、モデルやベクトルストアでは
ありません。バックエンドの検索サービス・モデル重み・非公開インデックスは
サーバー側に留まり、本ツールと共に公開されることはありません。）

## できること／できないこと

**できること**：自然言語で判決を検索 → 構造化リスト・判決理由の抜粋・引用リンクを
取得 → バンドルにまとめて自分の AI に渡す。AI が生成した回答に対する
バンドルレベルの引用チェック。

**できないこと**：本ツールは **LLM を呼び出さず、法的意見を生成せず、いかなる
モデル出力も保証しません**。回答はあなたが選んだ AI（ChatGPT / Claude / Gemini /
ローカルモデル）が生成します。

### 内蔵の引用チェックで検証できること

`check` は**バンドルレベルのベストエフォート**文字列チェックで、以下のみを
検証します：

- 回答が引用した判決番号が**バンドル内にあるか**（「バンドル外の番号を引用」＝
  捏造の疑いを検出）；
- バンドル外あるいは実在しない判決の引用；
- **引用文の存在（バンドルレベル）**：回答が「裁判所いわく……」とする逐語文が、
  バンドルテキストの**どこか**に存在するか。

### 検証**できない**こと（重要）

- 引用文が**回答の指すその判決**に由来するか（存在チェックは「バンドル全体の
  どこかにあるか」のみを見て、特定判決に紐付けません）；
- 裁判所の判示を**正しく読めているか**；
- **当事者の主張**（原告・被告・上訴人）を**裁判所の見解**と取り違えていないか；
- **傍論**を判決の**中核的権威**として扱っていないか；
- 言い換え（パラフレーズ）型の判示ハルシネーション。

これらはいずれも判決全文を読まなければ判断できません — だからこそバンドルには
判決抜粋と検証指示が同梱され、下流モデル自身に照合を義務付けています。
**`pass` は「引用番号の同一性がバンドルと一致した」ことだけを意味し、「法的推論が
正しい」「引用文が確かにその判決に由来する」ことを意味しません。** また `check` は
**バンドル内のコンテンツ**とのみ比較します。法律偵探のデータベース全体では
ありません — 後で自分で判決全文を開いて回答を書き直しても、`check` が見るのは
当初パックされた抜粋だけです。

## インストール

```bash
pip install twlegalrag
```

依存は `httpx` / `typer` / `rich` のみ。LLM パッケージも API キーも不要です —
本ツールは LLM を呼び出しません。

## 使い方

```bash
# 1) 検索のみ — 該当判決をリスト表示
twlegalrag search "勞資 加班費" -n 5 --read

# 2) パック — 任意の AI に渡せるバンドルを生成 ★メインフロー
twlegalrag pack "車禍對方全責,我可以求償什麼?" -o bundle.json
#   → bundle.json を ChatGPT / Claude / Gemini に貼り付け、バンドル内の判決のみ
#     引用するよう指示する

# 3) 引用チェック — AI が生成した回答へのバンドルレベル検査
twlegalrag check bundle.json answer.txt

# 4) 条文と行政解釈の照会 (LLM なし)
twlegalrag law 民法 184
twlegalrag ref "台財稅第881945861號"
twlegalrag ref-search "扣繳義務人未依限申報之處罰" -n 5

# サービス稼働確認
twlegalrag health
```

`pack` が生成するバンドルには `query`、各判決の `citation_id`（J1, J2, ...）、
`citation_text`、`citation_url`、`doc_id`、Layer-1 リスト、`fulltext_excerpt`
（判決理由の抜粋、長さ上限あり）、`case_history`（データベース記録の審級関係、
v1.1）、`allowed_citations`、そして下流モデルにバンドル内判決のみの引用と
根拠のない命題の unverified 表示を明示的に義務付ける
`verification_instructions` が含まれます。stderr には AI USE NOTICE も
出力されます。

v1.1 以降、`verification_instructions` には**見解レイヤーのセルフチェック**
（OPINION-LAYER SELF-CHECK）が追加されています。下流モデルは回答後に以下を
一つずつ確認しなければなりません：(a) ある判決に帰属させた判示がその判決の
抜粋に実際に存在すること（他の判決のものでも推論でもないこと）；(b) 裁判結果の
方向（勝訴／敗訴／破棄／棄却／差戻し）が逆転していないこと；(c) `case_history`
で破棄と表示された判決を現行有効な判示として引用していないこと。これは `check` の
バンドルレベル番号チェックを補完します — 事件番号が実在しても判示が実在するとは
限らず、見解レイヤーの照合は**全文を読んだモデル**にしかできません。これらの
ルールはその義務を各バンドルに書き込むものです。

`allowed_citations` は「引用可能な判決」のホワイトリストで、**理由全文を実際に
読み込んだ判決のみ**を含みます。CLI の `pack` は返された全判決を読み込むため、
両者は常に一致します。ホスト型 Remote MCP の `search_bundle`（`/v1/pack`）では
`read_top < max_results` の場合、上位 `read_top` 件のみ全文読み込みされ、残りは
閲覧用に `judgments` に残るものの `unread_candidates` に移されます（authority では
なく、裁判所の論理として引用不可）。詳細は
[`docs/mcp-anchor.md`](docs/mcp-anchor.ja.md)。

## 設定（任意）

デフォルトで公開エンドポイント `https://tlr.dr-legal.com.tw` に接続し、キー不要で
使えます。サービス側から API キーを発行された場合は、環境変数または
`~/.twlegalrag/config.toml`（git-ignore 済み — **絶対に** commit しないこと）に
設定します：

```bash
export TWLEGALRAG_TLR_BASE_URL=https://tlr.dr-legal.com.tw   # デフォルト
export TWLEGALRAG_TLR_API_KEY=...   # 任意；公開エンドポイントには不要（企業向けキーのみ）
```

```toml
[tlr]
# base_url = "https://tlr.dr-legal.com.tw"
# api_key  = "..."
```

## プライバシーとデータの流れ

まず、**TLR サーバーを決して経由しないもの**：

- あなたと AI（Claude / ChatGPT / ローカルモデル）との**会話全文、アップロードした
  文書、AI が生成した回答**は、すべてあなたと AI プロバイダーの間で完結し、TLR を
  **一切経由しません**。TLR は retrieval-only のサーバーであり、受け取るのは
  あなたの AI クライアントが送信を決めた**検索クエリ文字列**と、その後の判決文書の
  取得リクエストだけです。
- **アカウント登録不要**：公開 REST エンドポイントはキーなしで使え、本サービスに
  ユーザーアカウントの仕組みはなく、クエリはいかなるアカウント身元にも
  紐付きません。
- 判決データ自体は台湾の**公開**裁判文書であり、レスポンスに非公開の個人情報は
  含まれません。

ネットワークを流れるものとして理解しておくべきこと：

- あなたの**検索語・質問**は判決取得のため TLR 検索エンドポイント
  （`https://tlr.dr-legal.com.tw`）に送信されます。
- **TLR は検索品質分析のため、クエリ文字列・タイムスタンプ・IP 由来のメタデータ・
  結果件数を記録することがあります。個人の機密や秘匿すべき事実を送信しないで
  ください。クエリが生成モデルの学習に使われることはありません。**
- 本ツールは **LLM を呼び出さず、サーバー側トークンも使いません**。バンドルを
  自分で AI に渡す場合、その通信と費用は**あなたと選んだ AI プロバイダーの間**で
  発生し、本ツールとは無関係です。
- エンドポイントの API キー（ある場合）は環境変数に置き、設定ファイルを commit
  しないでください。

## 引用チェックの仕組み

`twlegalrag/faithful/` は**依存ゼロの純関数群**です（標準ライブラリの `re` +
`unicodedata` のみ）。回答テキストとバンドル内の判決抜粋を与えると
`pass` / `needs_review` / `fail` を返します。設計は保守的で、確信が持てない場合は
`fail` ではなく `needs_review` を返して誤検知を抑えます。**LLM を呼び出さず、
データベースにも触れない**、決定論的な文字列分析です。

⚠️ このディレクトリは内部コードの**スナップショット**であり、一部の関数
（`check_party_as_court` / `run_all_checks` など）は CLI では**使われていません**。
それらの存在は CLI が見解レイヤー／意味検証をできることを**意味しません** —
CLI が使うのはバンドルレベルの 2 チェックのみです。ファイル一覧を機能一覧と
読まないでください。詳細は `twlegalrag/faithful/VENDORED.md`。

## 2026-08-25 ホストサービス更新（MCP / REST）

- bundle レスポンスに `result_token` を付帯：bundle 内の任意の判決を全文照会可能。
- 各結果に `hit_excerpt`（ヒット箇所プレビュー）を追加。引用は理由全文に基づくこと。
- `get_judgment_fulltext` が `excerpt_offset` ページングに対応。長文判決も最後まで読了可能。
- 語彙検索モード（`keyword` / `phrase`）を改善。概念的な質問には `hybrid` を推奨。

CLI **v2.1.0** も対応：`pack` が長文判決を最後までページングして読了し、
`hit_excerpt` を bundle に含めます。

## その他の接続方法（同一 TLR バックエンド）

この CLI は TLR 検索サービスを使う方法の一つです。同じバックエンド
`tlr.dr-legal.com.tw` は、**Remote MCP** 経由で判決検索を AI ツールに直接組み込む
こともできます。いずれも同じ MCP エンドポイント
`https://tlr.dr-legal.com.tw/mcp` を設定し、接続時に OAuth が自動完了します
（動的登録、API キーの申請・設定は不要）：

- **Claude（Remote MCP）**：Settings → Connectors → Add custom connector で
  URL に `https://tlr.dr-legal.com.tw/mcp` を設定。
- **ChatGPT（MCP コネクタ）**：Connectors でカスタム MCP サーバーを追加し、
  URL に `https://tlr.dr-legal.com.tw/mcp` を設定。
- **Gemini CLI（Remote MCP）**：`~/.gemini/settings.json` の `mcpServers` に
  `{"tlr": {"httpUrl": "https://tlr.dr-legal.com.tw/mcp"}}` を追加し、CLI 内で
  `/mcp auth tlr` を実行して認可します。手順の詳細は
  [ユーザーガイド](docs/USER_GUIDE_ja.md) を参照。
- **Microsoft 365 Copilot / Copilot Studio（MCP）**：Copilot Studio または
  Agent Builder で agent に MCP ツールを追加し、Server URL に
  `https://tlr.dr-legal.com.tw/mcp` を設定、認証は OAuth 2.0 の動的探索
  (dynamic discovery) を選択します。アプリの事前登録は不要です。手順の詳細は
  [ユーザーガイド](docs/USER_GUIDE_ja.md) を参照。
- **Claude Code（Skill、MCP ではなく CLI 経由）**：本リポジトリには
  [`skills/tw-legal-rag/`](skills/tw-legal-rag/) に既製の skill が同梱されて
  います。フォルダごとプロジェクトの `.claude/skills/` に置くだけです。本 CLI の
  `pack` サブコマンドをラップし、台湾の判決・法的論拠について質問すると Claude が
  自動検索し、バンドル内の `citation_id` のみ引用するよう義務付けます。同梱の
  `scripts/search_judgments.py` は実行ファイルを自動検出し、Windows の 2 つの
  落とし穴にも対応しています（skill 内の `SKILL.md` 参照）。

Remote MCP インターフェースには現在 6 つのツールがあります：

| ツール | 機能 | 追加時期 | 本 CLI |
|--------|------|:-------:|:------:|
| `search_bundle` | 検索＋理由全文読込を 1 回で実行し、引用ホワイトリスト付き bundle を返却（推奨エントリポイント） | 2026-06 | ✅ |
| `search_judgments` | 判決検索（構造化 listing。完全な事件番号は完全一致照会に自動切替） | 2026-05 | ✅ |
| `get_judgment_fulltext` | 判決理由の全文（審級関係 `case_history` 付き） | 2026-05 | ✅ |
| `get_legal_reference` | 行政解釈の文書番号による正確検索、効力状態付き | 2026-08 | ✅ `ref` |
| `search_legal_references` | 行政解釈のセマンティック検索 | 2026-08 | ✅ `ref-search` |
| `get_law_article` | 現行法条の完全一致照会と条番号検証 | 2026-09 | ✅ `law` |

各ツールの入出力契約は [`docs/mcp-anchor.md`](docs/mcp-anchor.ja.md) を参照。

本サーバーは公式 [MCP Server Registry](https://registry.modelcontextprotocol.io/) に
`io.github.aa0101181514/tw-legal-rag` として登録されています。

CLI・MCP・Claude Code skill のいずれを使っても、回答を生成するのは**あなた自身の
AI** です。本サービスが提供するのは判決コンテンツと検証可能な引用リンクのみです。

## アーキテクチャ

```
あなたの質問
   │
[検索]  TLR /v1/search   ──►  Layer-1 リスト + result_token
   │    TLR /v1/fulltext ──►  判決ごとの理由抜粋（上限あり）
   │
[パック] pack ──► bundle.json (citation_id / allowed_citations / 検証ルール)
   │             └─► 自分の AI ツールに渡す
   │
[検査]  check ──► バンドルレベル引用チェック（バンドル内外 + バンドル内引用文存在）
```

判決コーパス・embedding・検索ロジックはサーバー側にあり、**本リポジトリには
含まれません**。この CLI は公開クライアント兼引用チェックツールです。

## 免責事項

本ツールは分析補助であり、法的助言でも弁護士でもありません。引用された判決の
全文は必ず自分で読んでください。API 経由で取得する判決は台湾の公開裁判データで
あり、その利用についてはあなた自身が責任を負います。

## License

v2.0.0 以降は **Elastic License 2.0（ELv2）** です。商用・社内利用を含め、
使用・複製・改変・再配布は自由ですが、本ソフトウェア自体を第三者向けの
ホスティング/マネージドサービスとして提供することはできません。
v1.2.2 までのバージョンは MIT のままです。

ホスティング API と判決コーパスはコードライセンスの対象外です
（[`TERMS.md`](https://github.com/aa0101181514/tw-legal-rag/blob/main/TERMS.md) 参照）。プロジェクト名とロゴはライセンスされません
（[`TRADEMARK.md`](https://github.com/aa0101181514/tw-legal-rag/blob/main/TRADEMARK.md) 参照）。本プロジェクトは外部 pull request を
受け付けていません（[`CONTRIBUTING.md`](https://github.com/aa0101181514/tw-legal-rag/blob/main/CONTRIBUTING.md) 参照）。
